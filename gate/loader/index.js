      //||------------------------------------------------------------------------------------------------||
      //|| Import
      //||------------------------------------------------------------------------------------------------||

      import { ComplyAge } from "./ComplyAge.js";

      //||------------------------------------------------------------------------------------------------||
      //|| Markers
      //||------------------------------------------------------------------------------------------------||

      const COMPLYAGE_SETUP = {            
            clientId    : "{{CLIENT_ID}}",
            debug       : ("{{DEBUG}}" === "true"),
            sessionName : "complyage_session",
            version     : parseFloat("{{VERSION}}"),
            urlGate     : "{{GATEURL}}",
            urlOAuth    : "{{OAUTHURL}}",
      };

      //||------------------------------------------------------------------------------------------------||
      //|| Entry Point / Start
      //||------------------------------------------------------------------------------------------------||

      (async () => {
            const ca = new ComplyAge(COMPLYAGE_SETUP);
            ca.authorize();
      })();
